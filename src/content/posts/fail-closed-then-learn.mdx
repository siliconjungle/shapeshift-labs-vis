Failing closed sounds conservative.

It is.

But it should not be passive. A blocked merge should teach the system something. It should become a sharper row, a focused fixture, a route, a lease, a gate, or a question.

Fail closed, then learn.

<SemanticMergeConservativeChart caption="A refusal is useful when it turns uncertainty into the next proof route." />

## Refusal Is A Decision

There is a difference between confusion and refusal.

Confusion says:

```txt
I do not know what happened.
```

A good fail-closed decision says:

```txt
this claim is not admitted
because this proof is missing
for this surface
and the next route is this
```

That shape matters because it gives the next worker a smaller job.

## The Route After Block

Blocked work should not disappear into a pile.

<RouteDecisionTable
  eyebrow="Fail Closed"
  title="Refusal Becomes Work"
  rows={[
    {
      route: 'Add row',
      signal: 'The surface is real but uncounted',
      requires: 'Name the claim and proof level',
      produces: 'A stronger denominator',
      tone: 'split',
    },
    {
      route: 'Add fixture',
      signal: 'The case should always pass or always fail',
      requires: 'Minimal source pair and expected decision',
      produces: 'Regression evidence',
      tone: 'gate',
    },
    {
      route: 'Ask',
      signal: 'The missing part is authority, not evidence',
      requires: 'Typed question with consequences',
      produces: 'Decision node',
      tone: 'ask',
    },
    {
      route: 'Rerun',
      signal: 'Evidence is stale or too broad',
      requires: 'Current head and narrower proof request',
      produces: 'Fresh candidate',
      tone: 'rerun',
    },
    {
      route: 'Block',
      signal: 'The candidate violates a boundary',
      requires: 'Conflict and reason code',
      produces: 'Durable non-admission',
      tone: 'block',
    },
  ]}
  caption="Failing closed is productive when the output is a route, not just a stop sign."
/>

The refusal is part of the system state.

It should be replayable.

## Proof Gets Sharper

The first proof request is often too broad.

```txt
prove the UI still works
prove the CSS is fine
prove the type change is safe
prove the branch can merge
```

After a refusal, the proof can become sharper:

```txt
prove this selector still reaches this target
prove this runtime capsule has DOM, style, layout, focus, and a11y hashes
prove this React list has unique stable keys
prove this public type still emits the expected declaration shape
```

The system learns by turning vague uncertainty into typed requirements.

<EvidenceLadder
  levels={[
    {
      name: 'Blocked claim',
      strength: 'refused',
      proves: 'The current evidence is not allowed to admit the merge.',
      limit: 'Does not say the idea is wrong.',
      route: 'Classify missing proof',
      tone: 'note',
    },
    {
      name: 'Focused fixture',
      strength: 'repeatable',
      proves: 'The exact case now has an expected decision.',
      limit: 'Does not cover every related case.',
      route: 'Add gate',
      tone: 'focused',
    },
    {
      name: 'Matrix row',
      strength: 'counted',
      proves: 'The surface is part of the denominator.',
      limit: 'Still needs real evidence per candidate.',
      route: 'Route future work',
      tone: 'static',
    },
    {
      name: 'Admission rule',
      strength: 'enforced',
      proves: 'Future candidates cannot bypass the boundary.',
      limit: 'Can still be too narrow and need expansion.',
      route: 'Apply only when passed',
      tone: 'decision',
    },
  ]}
  caption="Learning happens when a refusal becomes fixture, matrix row, gate, and admission rule."
/>

## Refusal Preserves Useful Work

Failing closed does not mean throwing everything away.

A candidate can be blocked while parts of its evidence remain useful.

The parser proof may be good. The type proof may be good. The runtime proof may be missing. The route should preserve the good parts and ask for the missing one.

That is how the system avoids both extremes:

```txt
do not blindly merge
do not blindly discard
```

## The Mental Model

A blocked merge is not a dead end.

It is the system saying:

```txt
this claim is too large for the proof
so make the claim smaller
or make the proof stronger
```

That is how conservative merge becomes an engine for learning.
